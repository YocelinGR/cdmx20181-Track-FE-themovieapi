const {initializing} = require('../src/js/authentications');

describe('initializing, is a function', () => {
  it('El objeto config es un objeto', () => {
    expect(typeof(config)).toBe('undefined');
  });
});
